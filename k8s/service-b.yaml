apiVersion: apps/v1
kind: Deployment
metadata:
  name: service-b
  namespace: microservices-demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: service-b
  template:
    metadata:
      labels:
        app: service-b
    spec:
      containers:
      - name: service-b
        image: ghcr.io/jasontanks/service-b:latest
        ports:
        - containerPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: service-b
  namespace: microservices-demo
spec:
  selector:
    app: service-b
  ports:
  - port: 80
    targetPort: 3000
